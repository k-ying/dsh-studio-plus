# DSH Studio 路线图与当前对标结论

[English](ROADMAP.md)

更新时间：2026-09-13。最新对标仓库为 `dataelement/dsh-desktop`，提交
`c52f450d61a0efd33e63a9e8efa629b3cfe3cd9e`，官方 Harness 当前提交为
`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`（`dsh-v0.1.1-rc.2`）。对标项目
package 自报 0.1.1；本次仍以提交 SHA 作为比较依据。我方比较起点为
`9d608e7245e74662a67fe754222fd1b845270092`。

产品深度复核还抽样了 `vastsa/PI-Desktop`、`jasonsuhari/gridbash`、
`wess/sinclair`、`BraydenPB/agent-grid`、`limboo-ai/limboo` 和
`jpdlr/hydra`。这些项目只用于提炼交互、隔离和恢复模式；README 中的路线图如果没有
对应代码或测试，不会被当成已交付能力。

这里不记录会快速失效的 Star、下载量，也不把“流水线支持”写成“公网渠道已经上线”。
每项状态只由代码、测试、已发布资产或明确的外部条件支撑。

## 结论

功能面已经不是“追平一个桌面壳”：DSH Studio 同时提供 Windows/macOS/Linux、轻量与完整
离线包、插件市场、受认证的手机远程、多 Profile、内置终端、多窗口、会话全文搜索、Token
与费用统计、会话导出、命令面板、崩溃/诊断恢复和受限插件协议。

当前真正的差距集中在四处：

1. **分发信任与触达**：系统代码签名凭据、外部包管理器首次提交、稳定镜像与社区触达。
2. **真机验收深度**：CI 能证明跨平台编译和 headless 行为，但不能替代 macOS 真机、签名安装、
   更新和系统集成验收。
3. **打包 UI 自动化**：已有完整逻辑覆盖和真实 Harness 启动门禁，仍需给最终安装包增加可重复
   的 WebView/窗口交互冒烟。
4. **并行代理工作区深度**：原生工作树发现与创建已经交付；仍缺少持久分屏布局，以及面向
   多代理结果、带保护合同的原生审查/合并界面。

## 当前能力矩阵

| 能力             | 对标最新 `c52f450d`                 | DSH Studio 当前状态                                                                                      |
| ---------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 零系统 Node 启动 | 内置 Electron/运行时                | 轻量版按需下载并校验官方 Node；完整离线版携带 SHA-256 固定的 Node、Harness、pnpm                         |
| 真实运行时门禁   | 有 Profile boot smoke               | 冷安装完整 511 包图，在 Windows/Linux/macOS CI 启动真实 Profile、Loader、Web 与 Host 探针                |
| 展示模式         | Compatibility / Extended / Advanced | 同样三种；Extended 保留完整上游页面并叠加紧凑原生工具栏                                                  |
| 固定 Web 端口    | 支持                                | 默认随机，也可保存 1024–65535；占用时在启动 Node 前明确失败                                              |
| 启动恢复         | 注入恢复资源                        | 12 秒原生 watchdog + 不依赖 React/Harness/Node/网络的静态恢复窗口                                        |
| Profile          | 管理、切换和桌面服务                | 新建、复制、重命名、删除、导入导出、差异比较、跨窗口切换                                                 |
| Host 插件能力    | 可变 Profile 与受管 pnpm 服务       | Host Protocol 1 只读版本/Profile 清单；修改继续走 Protocol 3 的可见审查、回执和回滚链                    |
| 插件市场         | 内置 Community Market               | 多来源目录、分页/限额、精确 npm 复核、预览 token、完整性回执、事务回滚、错误弹窗                         |
| 目录健康         | 来源管理与测试                      | npm、dshfind、1024Store、自定义来源的原生协议/延迟/可安装性健康检查                                      |
| 内置终端         | 有                                  | PTY + Unicode 11 + 进程树回收；不会弹外部 CMD                                                            |
| 并行代理工作区   | 上游子代理                          | 已有 Harness 子代理、多终端标签和原生 Git Worktree 隔离；持久分屏与受保护编排审查仍属于计划              |
| 手机远程         | 计划中                              | 已交付：Harness 仍在回环，独立 LAN 网关、一次性二维码、每设备可撤销凭据                                  |
| 会话能力         | 上游页面为主                        | 本地全文搜索、跨项目筛选、按模型 Token/费用统计、Markdown/HTML/JSON 导出                                 |
| 用量控制         | 上游会话界面                        | 本地月预算状态、完整定价保护、可防表格公式注入的逐日 CSV 趋势导出                                        |
| 多窗口           | 桌面窗口管理                        | 同一 Harness 上并行窗口，独立任务界面与窗口位置记忆                                                      |
| 平台             | Windows x64、macOS Universal        | Windows x64、Linux x64、macOS Intel/Apple Silicon，含 Universal 轻量镜像                                 |
| 进程生命周期     | Electron 运行时管理                 | Windows Job Object / Unix 进程组回收整棵 Node、PTY 与工具子进程树                                        |
| 诊断             | 日志/恢复能力                       | 脱敏 ZIP、轮转限额日志、崩溃证据、Windows minidump、无界面导出命令                                       |
| 持久写入         | 文件恢复合同                        | 同目录唯一暂存、完整落盘、Windows/Unix 原子替换，覆盖导出与持久状态                                      |
| 无障碍           | 原生框架细化                        | 键盘/对话框/表单语义门禁、减少动画、强制颜色和中英文人工验收矩阵                                         |
| 首屏负载         | Electron 内置界面                   | 终端仿真延迟加载；入口 JS 强制低于 450 KiB，当前为 364,959 字节                                          |
| 更新             | 桌面更新                            | Tauri 签名强制、GitHub 主源 + Pages 清单回退、资产矩阵与 SHA256SUMS 门禁                                 |
| 包管理器         | 官网安装包                          | Scoop 已可用；winget/Homebrew/AUR/Flathub 清单会随发布生成并在原生工具链验证，但尚未全部完成外部首次提交 |

## 本轮补齐并已验证

- `2c0761f`：全量冷安装 + 真实 Profile/Loader/Web 端口门禁，三大桌面系统进入 CI 矩阵。
- `895b167`：renderer 首次提交握手、watchdog、静态恢复页、脱敏诊断导出。
- `9fe39ab`：可选固定回环端口、持久设置、占用预检和错误链路。
- `026f3df`：第三种 Extended 展示面；Workbench/Profile Manager 按需加载，主入口包减小。
- `2de525a`：Host Protocol 1、SDK 类型、受限 Profile 清单、生命周期关闭与真实 Loader 探针。
- `ad588d9`：取消操作后可靠释放 Profile/插件任务锁。
- `fd193d3`：带 SHA-256 校验的 Profile 备份和恢复预览。
- `472e520`：原生目录协议健康检查。
- `ae86fc5`：更新确认绑定到用户实际审查的精确版本。
- `f293373`：本地预算状态与防表格公式注入的趋势导出。
- `31a665a`：显式操作统一可复制错误弹窗，后台刷新保持安静。
- `6ad66d5`、`4e62d62`：无障碍门禁、强制颜色和表单辅助名称。
- `39bc348`：导出、设置、Profile、插件恢复状态统一原子写入。
- `ff21525`：终端仿真切包与发布构建体积门禁。

这些提交最初随 v0.9.2 正式版本发布，并在 v0.9.7 中持续保留（兼容性基线仍记录 v0.9.4）；每次均完成流水线、公开资产、更新签名和校验和核验。

## 下一阶段：发布后继续完成

### P0 · 信任和真机

- **系统签名实证**：发布流水线已经支持 Azure Artifact Signing、Apple Developer ID、公证和
  stapler，但只有配置真实凭据并在正式产物验证通过后，才能宣称对应资产具有系统发布者签名。
- **macOS 真机矩阵**：至少覆盖 Intel 与 Apple Silicon 的首次安装、Gatekeeper、登录项、通知、
  目录选择、终端、完整离线版和应用内升级。目前没有 Apple 设备，不能把 CI 当作真机结论。
- **升级矩阵**：用最近两个正式版本分别覆盖轻量→轻量、完整→轻量、损坏更新清单、离线更新、
  更新后 Profile/插件/凭据保持。

### P0 · 分发

- 提交并维护 winget、独立 Homebrew tap、AUR；Flathub 只有在宿主开发工具边界解决后才对外
  宣称等价支持。
- 建立可验证的大陆镜像：只镜像原字节，独立同步 `SHA256SUMS.txt` 和签名，监测延迟与错误，
  不把临时 GitHub 代理写成官方渠道。
- 官网展示真实渠道状态、版本、架构、轻量/完整区别和校验方式；状态来自发布资产而不是手工文案。

### P1 · 最终安装包自动化

- Windows 安装包：安装→首次引导→运行时准备→启动→新建终端→切换三种展示面→重启→卸载。
- Linux AppImage/deb/rpm：WebKit 启动、PTY、文件选择、托盘与进程树回收。
- macOS：待真机和签名凭据具备后加入相同流程；在此之前保持“构建/单测通过，未真机验证”的标注。
- 把截图、WebView 控制台、Harness 尾日志和诊断包作为失败产物上传，不能只留下一个退出码。

### P1 · 隔离代理工作区

- 原生工作树发现、仓库预检、安全分支身份和防碰撞同级目录创建已经交付；仍需补持久所有权回执、
  中断操作恢复与受保护移除。Studio 仍绝不删除有未提交修改或不属于自己的工作树。
- 每个受管终端/会话绑定唯一工作区身份。重启后恢复布局和启动意图，但不能把被应用退出回收的
  子进程描述成“仍在运行”。
- 增加可持久化递归分屏树、完整键盘导航和明确窗格上限；一次启动大量窗格前展示资源估算并确认。
- 合并前按工作区和代理审查文件、diff、测试证据、冲突状态与受保护回滚；只有校验修改前身份和
  脏工作树状态后，“回滚”才是安全合同，而不只是一个按钮。

### P1 · 插件生态

- 随下一次正式版本发布 SDK 与 Host Protocol 1 文档；增加第三方 fixture 仓库和兼容性测试模板。
- 后续为提供者发布机器可读健康回执。本地原生协议/延迟/可安装性检查已经交付；HTML 网站仍不能冒充安装 API。
- Host 协议只在真实用例出现时增加窄能力。包管理、任意命令和原生句柄不进入通用只读服务，
  需要修改的操作继续由用户可见的 Protocol 3 审查、一次性意图、回执与回滚控制。

### P2 · 产品深化

- 日期趋势、本地预算状态和防表格公式注入的 CSV 导出已经交付。
- 不携带凭据的 Profile 可验证备份/恢复预览已经交付。
- 键盘/表单/对话框结构门禁、减少动画、强制颜色，以及屏幕阅读器/200% 缩放人工矩阵已经交付；辅助技术真机运行仍属于候选版本验收。
- 提供可选、明确同意、可关闭的匿名可靠性指标；在隐私设计和数据保留规则完成前不接入遥测 SDK。
- 只有工作树和窗格所有权合同完成后，才提供窄范围、受认证的本地编排控制面：允许有上限地
  新建/检查/停止会话和投递消息；任意桌面 IPC、凭据及未经审查的包修改不得进入该协议。

## 发布判定

未来一次版本只有同时满足以下条件才可发布：工作树和版本差异已审计；前端与 Rust 覆盖率门禁、
Clippy、生产构建、打包测试通过；完整运行时在三平台冷安装并真实启动；对应平台资产、更新签名、
SHA-256 与系统签名状态逐项核验；Release 说明只写由提交、测试和实际产物支持的更新与修复。
