name: Package channels

on:
  workflow_dispatch:
  workflow_run:
    workflows: [Release]
    types: [completed]

permissions:
  contents: write

concurrency:
  # Release, workflow_run, and manual backfills must share one queue. Using
  # run_id here allowed overlapping writers to race on website/latest.json.
  group: package-channels
  cancel-in-progress: false

jobs:
  generate:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    name: Regenerate manifests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22.19.0
      - name: Resolve published release tag
        id: release
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = workflow_run ] || [ "${{ github.event_name }}" = workflow_dispatch ]; then
            tag="$(gh release view --repo "$GITHUB_REPOSITORY" --json tagName --jq .tagName)"
          fi
          test -n "$tag"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: node packaging/generate.mjs "${{ steps.release.outputs.tag }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/upload-artifact@v4
        with:
          name: package-channel-manifests
          include-hidden-files: true
          path: |
            bucket/dsh-studio.json
            packaging/winget
            packaging/homebrew
            packaging/aur
            packaging/flathub
            website/latest.json

  winget:
    name: Validate WinGet
    needs: generate
    runs-on: windows-2025
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: package-channel-manifests
      - run: winget validate --manifest packaging\winget

  homebrew:
    name: Validate Homebrew Cask
    needs: generate
    runs-on: macos-15
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: package-channel-manifests
      - run: brew style packaging/homebrew/dsh-studio.rb
      - name: Audit Cask from a temporary tap
        run: |
          brew tap-new --no-git moresyl/dsh
          tap_root="$(brew --repository moresyl/dsh)"
          mkdir -p "$tap_root/Casks"
          cp packaging/homebrew/dsh-studio.rb "$tap_root/Casks/dsh-studio.rb"
          # This is an independent tap, not a submission to homebrew-cask, so
          # core's popularity threshold is not part of its validity. Platform
          # signing is optional until this repository has signing credentials,
          # so validation must not turn Gatekeeper into a release prerequisite.
          brew audit --cask --strict moresyl/dsh/dsh-studio

  aur:
    name: Validate AUR package
    needs: generate
    runs-on: ubuntu-22.04
    container: archlinux:base-devel
    steps:
      - run: pacman -Syu --noconfirm git namcap
      - uses: actions/download-artifact@v4
        with:
          name: package-channel-manifests
      - run: |
          useradd --create-home builder
          chown -R builder:builder "$GITHUB_WORKSPACE"
          runuser -u builder -- sh -c 'cd packaging/aur && makepkg --printsrcinfo > generated.SRCINFO'
          diff -u packaging/aur/.SRCINFO packaging/aur/generated.SRCINFO
          namcap packaging/aur/PKGBUILD

  flatpak:
    name: Build Flatpak manifest
    needs: generate
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: package-channel-manifests
      - run: |
          sudo apt-get update
          sudo apt-get install -y appstream-compose flatpak flatpak-builder
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak install --user -y flathub org.flatpak.Builder org.gnome.Platform//49 org.gnome.Sdk//49
          flatpak-builder --user --force-clean --disable-rofiles-fuse build-dir packaging/flathub/io.github.moresyl.dshstudio.yml
      - name: Validate AppStream with Flathub's linter
        run: |
          flatpak run --user --filesystem="$GITHUB_WORKSPACE" \
            --command=flatpak-builder-lint org.flatpak.Builder \
            appstream "$GITHUB_WORKSPACE/packaging/flathub/io.github.moresyl.dshstudio.metainfo.xml"

  commit:
    name: Publish generated manifests to main
    if: github.repository == 'Moresyl/dsh-studio'
    needs: [winget, homebrew, aur, flatpak]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
      - uses: actions/download-artifact@v4
        with:
          name: package-channel-manifests
      - name: Commit only validated channel manifests
        run: |
          git add -- \
            bucket/dsh-studio.json \
            packaging/winget \
            packaging/homebrew \
            packaging/aur \
            packaging/flathub \
            website/latest.json
          if git diff --cached --quiet; then
            echo "Channel manifests already match the published release."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          version="${{ github.event.release.tag_name }}"
          if [ -z "$version" ]; then version="latest release"; fi
          git commit -m "chore: update package channels for $version"
          git push origin HEAD:main
